#ifndef SPINE_CONFIG_APPLY_H
#define SPINE_CONFIG_APPLY_H

#include "common.h"
#include "spine.h"
#include "config_builder.h"

void config_apply_runtime(config_t *target, const RuntimeConfigDraft *draft);

#endif
