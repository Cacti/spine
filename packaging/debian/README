To build a .deb from source, symlink or copy this directory to the
source root before running dpkg-buildpackage:

    ln -s packaging/debian debian
    dpkg-buildpackage -us -uc -b

The Debian packaging rules use the native CMake + Ninja build path.
